# Idea Bubble 无需 AI Provider、API Key 或模型环境变量。
#
# 真实 AI 使用 BYOK（Bring Your Own Key）：每位访问者在应用的“模型设置”中
# 填写自己的 Provider、API Key、Base URL 与各任务模型。配置只保存在该浏览器、
# 该站点 Origin 的 localStorage 中，真实请求由浏览器直接发送给所选 Provider。
#
# 不要在 Vercel 或本文件中配置用户密钥，也不要创建 NEXT_PUBLIC_*_API_KEY。
